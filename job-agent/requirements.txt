# Dzvonko — LangGraph Job Application Automation Agent
# Python 3.12+ (3.14 is fine; 3.11 fallback if wheels are missing)

# --- Web / API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- Agent orchestration ---
langgraph>=1.0
langchain-core>=0.3

# --- LLM gateway (OpenRouter is OpenAI-compatible) ---
httpx>=0.27

# --- Database ---
supabase>=2.10

# --- Browser automation (Phase 5) ---
playwright>=1.49

# --- Testing ---
pytest>=8.3
pytest-asyncio>=0.24
