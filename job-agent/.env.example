# --- Server ---
PORT=8080
ENVIRONMENT=development
LOG_LEVEL=INFO

# --- OpenRouter (OpenAI-compatible gateway) ---
OPENROUTER_API_KEY=sk-or-v1-REPLACE_ME
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=nvidia/nemotron-3-ultra-550b-a55b:free

# Different models for different workloads (optional)
MODEL_FAST=nvidia/nemotron-3.5-lightning:free
MODEL_REASONING=z-ai/glm-5.2:free
MODEL_EXTRACTION=minimax/minimax-m3:free

# Comma-separated spare keys for automatic rotation on 401/429
OPENROUTER_KEYS=sk-or-v1-REPLACE_ME,sk-or-v1-REPLACE_ME2

# --- Supabase (project: AGENT_Dzvonko-DB, ref wzeauydeiddxzcairuxm) ---
SUPABASE_URL=https://wzeauydeiddxzcairuxm.supabase.co
SUPABASE_ANON_KEY=REPLACE_ME               # publishable key (browser/client-safe)
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME       # secret key (server only)

# --- Email provider (Phase 6 - human-approved sends) ---
EMAIL_PROVIDER=smtp
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=janepetkovrabota@gmail.com
EMAIL_SMTP_PASSWORD=REPLACE_ME             # app password (never commit)

# --- Browser (Phase 5) ---
BROWSER_HEADLESS=true
BROWSER_TIMEOUT_MS=15000

# --- Upstash (Phase 8 + Box runtime) ---
UPSTASH_REDIS_URL=https://engaged-minnow-124446.upstash.io
UPSTASH_REDIS_TOKEN=REPLACE_ME

# --- App branding ---
AGENT_NAME=Dzvonko
X_TITLE="Za JobAPPLY"
HTTP_REFERER=https://za-jobapply.example
