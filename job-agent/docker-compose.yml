services:
  job-agent:
    build: .
    container_name: dzvonko
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - ENVIRONMENT=development
    volumes:
      - ./app:/app/app
    restart: unless-stopped
